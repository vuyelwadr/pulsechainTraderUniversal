
# Strategy Runner V8 — aggressive runner
# Defaults: exit_up=0.048, runner_frac=0.15, runner_trail=0.30
# Uses same cost model and logic as V7 but with more aggressive constants.
# (For brevity, import from V7 code structure would be ideal; here we keep a minimal stub.)
